{
  "name": "@tiagodinis/utils",
  "version": "0.0.12",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup index.ts --format esm --dts",
    "test": "tsc tests.ts --outDir temp && node temp/**/tests.js && rm -r temp"
  },
  "devDependencies": {
    "@types/react": "^18.2.12",
    "react": "^18.2.0",
    "tsup": "^6.7.0",
    "typescript": "^5.1.3"
  }
}
